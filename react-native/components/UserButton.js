import React from "react";
import {View} from "react-native";
const UserAddButton = ({title, onPress}) => {
  return (
    <View>
        {/* 
            1. перенаправляет на экран добавления юзера (AddUserScreen) || экран UserListScreen
        */}
    </View>
  );
}